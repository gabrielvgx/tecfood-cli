import { text } from '../template_option.js';

const mongo = {
    name: 'mongo',
    async execute(){
        // const { value: MONGO_CONTAINER_NAME } = await text("Container name", 'mongo-container');
        // const { value: MONGO_EXT_PORT_27017  } = await text("Porta mapeada para 27017", '27017');
        // const RESPONSE = {
        //     MONGO_CONTAINER_NAME,
        //     MONGO_EXT_PORT_27017,
        // };
        // return RESPONSE;
    }
}
export default mongo;